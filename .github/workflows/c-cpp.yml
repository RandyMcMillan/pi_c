name: make tests

on:
  push:
    branches: [ "master", "main", "**", "**/**" ]
  pull_request:
    branches: [ "master", "main", "**", "**/**" ]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3
    - name: make
      run: make
    - name: make tests
      run: make tests
    - name: pi
      run: ./pi
